package me.hsgamer.extrastorage.api.component;

/**
 * A component that can be reloaded at runtime.
 */
public interface Reloadable {

    /**
     * Reloads the component.
     */
    void reload();
}
